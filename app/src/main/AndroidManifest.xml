<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gw.kisansewa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:logo="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen"
            android:launchMode="singleInstance"/>

        <activity android:name=".FarmerRegister"
            android:launchMode="singleInstance"/>

        <activity android:name=".SellProducts"
            android:launchMode="singleInstance"/>

        <activity android:name=".BuyProducts"
            android:launchMode="singleInstance"     />
        <activity android:name=".ConfirmProductBuy"
            android:launchMode="singleInstance"/>
        <activity android:name=".Purchased"
            android:launchMode="singleInstance"/>
        <activity android:name=".Sold"
            android:launchMode="singleInstance"/>
        <activity android:name=".FarmerLogin"
            android:launchMode="singleInstance"/>

        <activity android:name=".SplashScreen"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>